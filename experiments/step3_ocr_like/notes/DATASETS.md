# Step3 Datasets

Priority starter pack:

1. synthetic text rendered as image
2. `nielsr/funsd` (forms, OCR + key-value context)
3. `CORD` / receipts (phase 2)
4. `DocVQA` (phase 2)

Current implemented benchmark:
- `synthetic_text_image` token recall baseline
- `FUNSD` token recall baseline

Rationale:
- no manual PDF supply required
- quick to run and verify extraction quality and latency.
